# 🔧 Локальное тестирование проекта

## Проблема: "Подключение не установлено"

Если вы видите эту ошибку при локальном тестировании, значит MySQL сервер не запущен.

## Решение

### 1. Запустите MySQL сервер

**Если используете XAMPP:**
1. Откройте **XAMPP Control Panel**
2. Нажмите **"Start"** рядом с **MySQL**
3. Должен загореться зелёный индикатор

**Если используете OpenServer:**
1. Откройте **OpenServer**
2. Нажмите на иконку OpenServer в трее
3. Выберите **"MySQL" → "Старт"**

**Если используете другой локальный сервер:**
- Запустите MySQL сервис через службы Windows или через панель управления вашего сервера

### 2. Создайте базу данных

1. Откройте **phpMyAdmin**:
   - XAMPP: http://localhost/phpmyadmin
   - OpenServer: http://openserver/phpmyadmin

2. Создайте базу данных:
   - Нажмите **"Создать"** (New) в левом меню
   - Имя базы: `shop_db`
   - Кодировка: `utf8mb4_unicode_ci`
   - Нажмите **"Создать"**

3. Импортируйте структуру:
   - Выберите базу `shop_db`
   - Перейдите на вкладку **"Импорт"** (Import)
   - Нажмите **"Выбрать файл"** и выберите `database.sql`
   - Нажмите **"Вперёд"** (Go)

**Или используйте setup.php:**
- Откройте в браузере: `http://localhost/ваша-папка/setup.php`
- Скрипт автоматически создаст таблицы и пользователя

### 3. Проверьте настройки в config.php

Убедитесь, что настройки правильные для локального сервера:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');  // Пустой для XAMPP, может быть 'root' для других
define('DB_NAME', 'shop_db');
```

**Для OpenServer:**
```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', 'root');  // Обычно 'root' для OpenServer
define('DB_NAME', 'shop_db');
```

### 4. Проверьте работу

1. Обновите страницу в браузере
2. Ошибка должна исчезнуть
3. Сайт должен работать

## Быстрая проверка

### Проверка подключения MySQL:

Создайте файл `test_db.php`:

```php
<?php
$conn = new mysqli('localhost', 'root', '');
if ($conn->connect_error) {
    die("MySQL не запущен: " . $conn->connect_error);
} else {
    echo "MySQL работает! ✅";
}
$conn->close();
?>
```

Откройте в браузере - если видите "MySQL работает!" - MySQL запущен.

## Типичные проблемы

### ❌ MySQL не запускается

**Решение:**
- Проверьте, не занят ли порт 3306 другим приложением
- Перезапустите компьютер
- Переустановите XAMPP/OpenServer

### ❌ Ошибка доступа (Access denied)

**Решение:**
- Проверьте пароль в config.php
- Для XAMPP обычно пустой пароль `''`
- Для OpenServer обычно `'root'`

### ❌ База данных не найдена

**Решение:**
- Убедитесь, что база `shop_db` создана
- Импортируйте database.sql
- Проверьте имя базы в config.php

### ❌ Порт 3306 занят

**Решение:**
- Остановите другие MySQL серверы
- Или измените порт MySQL в настройках XAMPP/OpenServer

## После успешной настройки

1. ✅ MySQL запущен
2. ✅ База данных создана
3. ✅ Таблицы импортированы
4. ✅ Настройки config.php правильные
5. ✅ Сайт работает локально

Теперь можете тестировать все функции сайта!

